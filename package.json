{
  "name": "agents-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "clean:browser-artifacts": "rm -rf .artifacts test-results playwright-report .playwright-mcp *-browser-qa.png *-desktop.png *-screenshot.png snapshot*.png",
    "validate:markdown-to-html-render": "node skills/markdown-to-html/scripts/validate-markdown-to-html-render.ts skills/markdown-to-html"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
