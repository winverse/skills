# Skill History

이 파일은 shared skill의 생명주기와 큰 변경만 기록하는 ledger다. 작은 문구 수정이나 단순 스타일 조정은 기록하지 않는다.

## Current Registry

| Skill | State | Risk tier | Last reviewed | Notes |
| --- | --- | --- | --- | --- |
| `show-skills` | `active` | low | 2026-05-12 | 현재 repo의 스킬 목록을 파일 시스템과 history에서 읽어 카테고리별로 보여주고, 작업에 맞는 스킬 조합을 추천하며, `skill.html`의 전체 HTML 카탈로그를 생성 스크립트와 validator로 동기화하는 탐색 스킬 |
| `web-research` | `critical` | high | 2026-05-18 | 현재성, 출처 검증, 추천, 규정, 기술 문서 조사에 쓰며, `web-research` 또는 alias 호출 자체를 explicit parallel sub-agent fan-out/delegation/parallel agent work 요청으로 해석하는 핵심 조사 스킬 |
| `skill-to-html` | `active` | medium | 2026-05-21 | `SKILL.md` 옆의 사용자용 `skill.html`을 한국어 우선 정적 요약으로 만든다. Markdown 기준 원문에서 사용 조건, 핵심 계약, 금지 경계, 파일, 검증 명령만 압축하며, 일반 설명어는 한국어로 번역하고, Markdown `raw HTML`/event handler/`javascript:` link/외부 script를 통과시키지 않는다. 버튼, inline JavaScript, 애니메이션, 외부 의존 없이 PC desktop에서 overflow와 text overlap을 확인하는 요약 스킬 |
| `karpathy-thinkings` | `active` | medium | 2026-05-12 | Karpathy식 코딩 에이전트 사고로 추측, 과설계, 주변 리팩터링, 약한 검증을 줄이는 구현 스킬 |
| `skill-update` | `active` | medium | 2026-05-20 | `docs/update-source-registry.md`를 단일 source registry로 읽고, `.gitmodules` 기반 vendored source와 workflow provenance-only primitive source id를 분리한 뒤 source, references, validator, visual guide, snippets, docs, history를 함께 맞추는 유지보수 스킬. 외부 dependency 전체 갱신 요청은 vendored plugin, repo-owned plugin, registry workflow primitive lane, workflow usage map을 모두 확인하는 dependency update sweep으로 처리하며, source URL/checked date는 registry에만 둔다. |
| `atomic-committer` | `active` | medium | 2026-05-20 | dirty git tree에서 실제값처럼 보이는 credential assignment와 private key를 차단하고, 반복 local/secret artifact는 `.gitignore`로 예방한 뒤 atomic commit 단위로 나누며, 사용자가 push 금지를 명시하지 않는 한 커밋 후 push까지 수행하는 스킬 |
| `pull-request` | `active` | medium | 2026-05-13 | GitHub PR의 branch/base/head, title/body, issue link, reviewer/label/draft 옵션과 `gh pr create` 실행 경계를 관리하는 스킬 |
| `project-structure` | `active` | medium | 2026-05-18 | frontend, backend, full-stack monorepo, desktop app, infrastructure-aware 구조와 folder-local AGENTS.md 목차, TypeScript ESM-only module 정책, 선택형 MongoDB Atlas, Supabase Postgres, psql/mongosh helper, Pulumi/Docker/AWS infra, 기본 stack/env/codegen/test/security/tool-boundary 정책을 정하는 스킬 |
| `project-workflow` | `active` | medium | 2026-05-20 | `plugins/project-workflow`의 plugin-bundled 초기 설정 skill이며 새 프로젝트나 큰 기획의 초기 셋팅을 도메인 문서, 제품 검증, ADR, `design.md`, PRD, 이슈 목록, TypeScript ESM-only 제약, 프로젝트 설정 확인, `workflow-state.md`, 병렬 `work-claims.md`, `feature-workflow` 인계까지 정리한다. 기존 top-level `skills/project-workflow` copy는 제거됐고 `plugins/project-workflow/skills/project-workflow/SKILL.md`가 정본이다. Matt Pocock gate는 `grill-with-docs`를 기본으로 쓰고, GStack/Superpowers 원본 primitive가 현재 runtime에서 가능하면 실제 호출하며 불가능하면 fallback 질문 루프로 대체한다. 큰 구현 인계 runner는 Codex-first `execute-phase.ts`로 두고, plugin 자체 개선은 `/goal` 반복 개선 루프로 fresh clone cycle, failure class 분류, 수정, commit/push, 재검증을 반복한다. 별도 언어 지정이 없으면 durable setup docs는 Korean-first artifact gate를 통과해야 한다. |
| `feature-workflow` | `active` | medium | 2026-05-20 | 초기 셋팅 이후 쓰는 별도 반복 개발 skill이다. 기존 PRD, issue, spec, bug report, acceptance criteria, ADR, `design.md`, `workflow-state.md`, `work-claims.md`를 기준으로 feature, bug fix, vertical slice를 TDD, TypeScript ESM-only preflight, 구현 계획, overlap block, review, QA/runtime evidence, document sync, improvement seed, completion reporting까지 끝낸다. |
| `sync-docs` | `active` | medium | 2026-05-12 | README, root/folder-local AGENTS, docs, snippets, history, skill 파일과 target project skill setup을 비교해 문서 최신화와 충돌 정리를 수행하는 스킬 |
| `transcript-polisher` | `active` | medium | 2026-05-13 | 전사본, 강의 대본, 자막, 회의록, 긴 산문을 코드 치환 없이 직접 읽고 source/output 구조, `polish` 분량, 불확실 용어를 검증하며 Claude Code `/goal`식 완료 조건 루프로 검토하는 스킬 |
| `agent-improvement-loop` | `active` | medium | 2026-05-12 | 소진형 실행 전 예/아니오로 사용자 확인을 받고, 답에 따라 safe backlog multi-agent batch 또는 단계별 ceiling review로 skill 호출성, validator, 문서 정합성, 일반 repo 품질을 개선하는 스킬 |
| `agent-eval-harness` | `active` | medium | 2026-05-12 | agent skill routing, cross-agent portability, workflow behavior, safety boundary, artifact hygiene, output quality, regression capture를 검증하고 file/link/schema 기반 deterministic check를 실행하는 초기 repo-local eval harness를 세팅하는 스킬 |
| `skill-plugin-test-loop` | `active` | medium | 2026-05-21 | 스킬, 플러그인, plugin-bundled skill을 새 복제본 또는 격리 복사본에서 실제 호출해 `cycle-NNN` 기록, 산출물 폴더, 제한 실행기 증거, 실패 분류, 출처 수정, `web-research` 기반 리서치 개선, 재실행을 남기는 반복 테스트 스킬 |
| `browser-qa` | `active` | medium | 2026-05-14 | Playwright/browser evidence로 렌더링, 콘솔, 네트워크, 접근성, 링크, viewport, text overlap을 검증하고, QA 종료 후 browser tab과 직접 시작한 server를 정리하는 스킬 |
| `code-review` | `active` | medium | 2026-05-12 | findings-first 코드 리뷰로 버그, 회귀, 테스트 누락, SRP/SOLID 경계, JS/TS 함수형 collection style, agent/tool surface 위험을 점검하는 스킬 |
| `design-review` | `active` | medium | 2026-05-21 | 여러 repo에 적용 가능한 제품 도메인/디자인 시스템 우선 UI 리뷰로 hierarchy, density, typography, state, accessibility, responsive order를 `S0-S4` severity, evidence matrix, criterion, user impact로 점검하는 스킬 |

## Event Log
| Date | Skill | Event | Lifecycle | Reason | Validation | Follow-up |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-05-21 | `skill-to-html` | Static summary contract adopted | `active -> active` | 인터랙티브와 애니메이션 중심 HTML이 오히려 읽기 어렵고, `fresh clone`, `failure class` 같은 일반 설명어가 화면에 영어로 노출되는 문제가 있어, `skill-to-html` 계약을 짧은 정적 요약과 강한 한국어 우선 규칙으로 바꿨다. 이제 화면 일반 설명어는 한국어로 번역하고 정확한 식별자만 원문으로 둔다. | skill-to-html validator, skill-to-html Playwright render validator, common HTML validator, skill-plugin-test-loop validator, Korean Markdown validator, repo validator | 다음 스킬 HTML 생성 때 script/button/animation이 되살아나거나 일반 설명어 영어가 화면에 노출되면 실패로 본다. |
| 2026-05-21 | `skill-plugin-test-loop` | Skill added | `none -> active` | 스킬과 플러그인을 실제 새 복제본 또는 격리 복사본에서 사람이 호출하는 것처럼 반복 검증하고, 결과를 끼워 맞추지 않도록 `cycle-NNN` 기록, 실제 첫 응답, 제한 실행기 증거, 실패 분류, 출처 수정, 재실행 계약을 분리된 스킬로 만들었다. | base skill validator, skill-plugin-test-loop validator, common HTML validator, repo validator | 실제 `project-workflow`나 plugin-bundled skill 검증에 적용하면서 검증 사례와 실패 분류가 과하거나 부족하지 않은지 점검한다. |
| 2026-05-21 | `skill-plugin-test-loop` | Research-informed improvement lane added | `active -> active` | 실제 호출 테스트만으로는 좋은 관행과 비교한 개선점을 놓칠 수 있어, 대상 스킬이 외부 best practice, 유명한 workflow, 최신 agent/tool 관행, security/QA guidance, upstream docs와 관련되면 `web-research`를 병렬 research-informed improvement lane으로 실행하고 `official/source`, `community/practice`, `counterexample/risk` source lane과 `adopt`/`adapt`/`reject`/`defer` 판단을 남긴 뒤 accepted delta를 반영하고 새 cycle을 돌리도록 했다. | skill-plugin-test-loop validator, show-skills catalog update, repo validator | 리서치 결과만으로 pass 처리하지 않고, 반영한 delta는 반드시 새 복제본 또는 격리 복사본 cycle로 다시 검증한다. |
| 2026-05-21 | `skill-plugin-test-loop` | External scratch fresh-clone gate clarified | `active -> active` | 완전한 검증은 target repo 안 임시 폴더가 아니라 Desktop 같은 repo 밖 external scratch root에서 `current/`만 매 cycle 삭제하고, commit/push된 GitHub 새 복제본을 `current/_github/<repo>`로 받아 실행하도록 명확히 했다. commit/push 전 변경은 preflight/local-only로만 기록하고 fresh clone pass로 보지 않는다. | skill-plugin-test-loop validator, skill HTML validator, repo validator | 다음 실제 검증은 commit/push 후 GitHub clone의 downloaded commit을 기록해야 한다. |
| 2026-05-21 | `skill-plugin-test-loop` | Required publish loop added | `active -> active` | `design-review` 검증 중 리서치 기반 개선을 찾고도 commit/push와 fresh clone 재실행을 생략할 뻔한 실패 패턴을 확인했다. 그래서 accepted delta가 있으면 `개선 -> 검증 -> atomic-committer로 commit/push -> Desktop 외부 루트에서 GitHub fresh clone cycle 재실행` 순서를 필수 publish loop로 추가하고, commit/push 전 cycle은 `preflight` 또는 `local-only`로만 기록하게 했다. | skill-plugin-test-loop validator, skill HTML validator | 다음 loop에서 accepted delta가 있으면 이 순서를 cycle summary와 최종 보고에 반드시 남긴다. |
| 2026-05-21 | `design-review` | Research-informed review contract hardened | `active -> active` | `skill-plugin-test-loop`로 `design-review`를 검증하면서 단순 findings-first만으로는 외부 best practice 반영과 재검증 loop가 약하다는 점을 확인했다. `web-research` 병렬 lane에서 NN/g heuristic evaluation과 severity rating, W3C/WCAG 접근성 기준, GOV.UK validation guidance를 비교해 `S0-S4` severity, evidence matrix, WCAG/heuristic criterion, user impact, recommendation, residual risk 계약을 추가했다. | design-review validator, common HTML validator, repo validator | commit/push 후 Desktop external scratch root에서 GitHub fresh clone cycle을 다시 실행해 개선된 계약을 실제 출력으로 검증한다. |
| 2026-05-20 | `project-workflow` | Expanded cycle gates added | `active -> active` | fresh clone cycle-019에서 CLI/no-browser case가 실제 구조와 검증 명령은 만들었지만 `# ledgerImportChecker PRD`, `# ledgerImportChecker workflow state`, `# setup validation`처럼 최상위 문서 제목이 영어 중심으로 생성됐고, 외부 Codex `resume`이 산출물 생성 후 final response 없이 멈추는 문제를 확인했다. 이에 따라 최상위 문서 제목까지 Korean-first artifact gate에 포함하고, UI dashboard, CLI/no-browser local data tool, API/external-risk mock service 확장 검증 케이스와 bounded resume timeout/final-response 실패 기준을 추가했다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, fresh clone cycle-019 failure record | 다음 cycle은 fresh clone 최신 commit에서 CLI/no-browser case를 다시 돌리고, 중요한 계약 변경 뒤에는 API/external-risk case까지 연속 확인한다. |
| 2026-05-20 | `project-workflow` | Korean-first heading gate hardened | `active -> active` | fresh clone cycle-017에서 실제 project shell, target validation, phase dry-run은 통과했지만 `workflow-state.md`에 `Agent Tool And Security Risk Gate`, `work-claims.md`에 `API lane` 같은 영어-only heading이 남는 실패를 확인했다. 위험 gate와 lane heading도 `도구/보안 위험 게이트(Agent Tool And Security Risk Gate)`, `API 작업 lane`처럼 한국어 heading 우선으로 쓰도록 계약, snippet, validator, visible guide를 보강했다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, fresh clone cycle-017 failure record | 다음 cycle은 `workflow-state.md` risk-gate heading과 `work-claims.md` lane heading까지 Korean-first gate로 검사한다. |
| 2026-05-20 | `skill-to-html` | Playwright render regression added | `active -> active` | `skill-to-html/skill.html` 첫 화면이 같은 변환 모델을 여러 section으로 반복해 난잡해지고, SVG dash animation 때문에 캡처 시 선 없이 화살촉만 보일 수 있음을 확인했다. 첫 hero를 하나의 고정 변환 레일로 줄이고, 중복된 변환 모델/애니메이션/tool rail section을 제거했으며, Playwright render spec으로 section 중복, hero arrow line, horizontal overflow, card readable width, click state를 검사하게 했다. | skill-to-html validator, skill-to-html Playwright render validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner | 다음 HTML 보강은 새 section을 추가하기 전에 기존 첫 도표가 같은 의미를 이미 담는지 먼저 확인한다. |
| 2026-05-20 | `skill-to-html` | Safety boundary diagram refined | `active -> active` | `금지와 허용`을 4칸 표처럼 나열하던 구간이 읽기 어렵고 구조를 보여주지 못해, 허용된 상호작용이 닫힌 template 안으로 들어오고 외부 의존은 boundary에서 차단되는 도표로 바꿨다. Playwright render spec은 `guard-grid` 회귀를 금지하고 `safety-boundary` 3열 구조를 확인한다. | skill-to-html validator, skill-to-html Playwright render validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner | safety/allow/deny 정보를 추가할 때 다시 평면 카드 표로 늘어놓지 말고 경계와 흐름을 먼저 그린다. |
| 2026-05-20 | `skill-to-html` | Markdown conversion model added | `active -> active` | Markdown 문서를 HTML로 바꿀 때 원문 줄바꿈을 화면에 복사하지 않고, `SKILL.md`를 source of truth로 읽어 heading/list/table/code/link 의미를 `skill IR`로 추출한 뒤 판단 보드, 흐름도, 파일 관계도, 검증 표, 근거 reveal로 매핑하도록 계약을 추가했다. Markdown `raw HTML`, event handler, `javascript:` link, 외부 script는 그대로 통과시키지 않고 repo-authored template의 닫힌 CSS/SVG/Web Animations API/짧은 inline JavaScript에서만 상호작용을 만든다. | skill-to-html validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check | 다음 skill HTML 생성 때 실제 화면이 Markdown 원문 순서 복사가 아니라 의미 구조와 component mapping으로 구성되는지 확인한다. |
| 2026-05-20 | `project-workflow` | Goal-driven improvement loop added | `active -> active` | project-workflow를 한 번 테스트하고 완료 선언하는 대신 `/goal`식 반복 개선 루프로 운영하도록 했다. GitHub fresh clone cycle을 돌리고, 실패를 `plugin contract`, `runner`, `test method`, `target artifact`, `environment/hook`으로 분류한 뒤 수정, validator, `atomic-committer` commit/push, 다음 cycle을 반복한다. 마지막 수정 이후 fresh clone cycle이 통과하고 blocker가 없을 때만 완료로 본다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | 다음 실제 개선 cycle부터는 cycle-summary에 goal, cycle count, failure class, goal status를 반드시 남긴다. |
| 2026-05-20 | `project-workflow` | Korean-first artifact gate hardened | `active -> active` | fresh clone cycle-010과 cycle-011에서 첫 응답, 실제 구조 생성, target validation, phase dry-run은 개선됐지만 durable setup docs와 `work-claims.md`, phase step 파일의 사람이 읽는 제목/필드 라벨/설명이 영어 중심으로 생성되는 실패를 확인했다. 이에 따라 target project가 다른 언어를 명시하지 않았으면 자연어 제목/본문/목록뿐 아니라 lane/field label과 phase step heading/body까지 한국어 우선으로 검사하는 hard gate를 추가했다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, fresh clone cycle-010/cycle-011 failure record | cycle-012부터는 `work-claims.md` lane/field label이나 phase step heading/body가 영어 중심이면 프로젝트 검증 명령이 통과해도 fail로 기록한다. |
| 2026-05-20 | `project-workflow` | Actual project shell gate added | `active -> active` | fresh clone cycle-012에서 첫 응답과 한국어 라벨은 개선됐지만 사용자가 `apps/web`, `apps/api`, `packages/domain` TypeScript monorepo shell을 확정했는데 `.scratch` Markdown만 만들고 실제 project structure와 validation command를 만들지 않는 실패를 확인했다. 이에 따라 구조가 확정된 setup은 최소 실행 shell, root package config, ESM tsconfig, source entrypoint, validation command를 만들거나 blocker로 보고하도록 계약을 추가했다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, fresh clone cycle-012 failure record | cycle-013부터는 docs-only setup을 pass로 보지 않고 실제 shell과 검증 명령을 확인한다. |
| 2026-05-20 | `atomic-committer` | Default push contract added | `active -> active` | commit 요청을 기본적으로 commit + push 요청으로 해석하도록 바꿨다. 여러 변경 단락은 logical changeset별 commit으로 분리하고, 사용자가 push 금지를 명시하지 않는 한 커밋 묶음 생성 후 push한다. remote, branch, upstream이 없으면 commit은 남기고 push 불가 사유를 보고한다. | atomic-committer validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | 실제 commit 요청에서 push 금지 표현, remote 없음, upstream 없음, 여러 changeset 분리 보고가 명확한지 점검한다. |
| 2026-05-20 | `project-workflow` | Execute phase regression tests expanded | `active -> active` | `harness_framework/scripts/test_execute.py`가 `execute.py` 리팩터링 안전망 역할을 하므로, 이를 TypeScript/Codex-first 계약에 맞게 `validate-execute-phase.ts`로 보강했다. dry-run 불변성, Codex docs 로딩, `CLAUDE.md` 제외, missing phase, pre-blocked/error, custom fallback 실행, output JSON, blocked status, max retry error 기록을 검증한다. | execute-phase validator, project-workflow plugin validator, source registry validator | 원본 테스트가 추가로 바뀌면 Python/Claude/auto-commit 동작은 복사하지 말고 Codex-first runner 계약에 맞는 deterministic case만 채택한다. |
| 2026-05-20 | `project-workflow` | Phase runner switched to Codex-first | `active -> active` | `harness_framework`의 `execute.py`에서 온 Claude 중심 실행 모델을 Codex용으로 정리했다. `execute-phase.ts`는 `--agent codex`와 explicit custom fallback만 허용하고, `CLAUDE.md` 로딩과 Claude 실행 예시는 제거했으며, docs/snippets/validator/source registry를 Codex-first boundary로 맞췄다. | execute-phase validator, project-workflow plugin validator, source registry validator, repo validator, Korean Markdown validator, skill HTML validator, agent eval runner | 다음 `skill-test` fresh clone cycle에서 Codex-first runner dry-run과 실제 project setup handoff가 기대대로 기록되는지 확인한다. |
| 2026-05-20 | `project-workflow` | Fresh clone test guard hardened | `active -> active` | `skill-test` 반복 cycle에서 wrapper cwd, sibling target write, `codex exec resume --last`, 다른 cwd resume가 실제 테스트를 왜곡하거나 local shared workspace를 수정할 수 있음을 확인했다. 테스트 방법 문서에 actual project root cwd, explicit session id, shared workspace guard, node_modules exclusion, fail 판정 기준을 추가하고 전용 validator가 이 guard를 검사하게 했다. | project-workflow plugin validator, repo validator, Korean Markdown validator, agent eval runner | 다음 fresh clone cycle에서는 첫 응답과 resume 모두 테스트 project root cwd에서 실행하고, shared workspace guard가 전후 동일한지 기록한다. |
| 2026-05-20 | `project-workflow` | Top-level compatibility copy removed | `active -> active` | `project-workflow` 정본을 `plugins/project-workflow/skills/project-workflow/SKILL.md` 하나로 줄여 단일 진실원 원칙을 지켰다. top-level `skills/project-workflow` copy와 copy drift 비교 validator를 제거하고, snippets, README, AGENTS, catalog, eval, repo validator가 plugin-bundled skill을 직접 가리키게 했다. | project-workflow plugin validator, source registry validator, plugin validator, repo validator, agent eval runner | 외부 프로젝트가 아직 옛 top-level 경로를 링크하면 plugin 정본 경로로 바꾼다. |
| 2026-05-20 | `skill-to-html` | Interactive visual-first HTML contract added | `active -> active` | `skill.html`이 텍스트 카드와 정적 표 중심으로 흐르지 않도록 첫 화면을 그림 먼저 보여주는 interactive visual scene으로 고정하고, mode switch, progressive disclosure, 의미 있는 animation, self-contained inline JavaScript, CSS/SVG/Web Animations API, local-bundled animation library 정책, 좁은 side panel 안의 4개 이상 계약 카드 grid 금지 기준을 추가했다. | skill-to-html validator, common HTML validator, Korean Markdown validator, source registry validator, repo validator, agent eval runner, Chrome headless PC interaction/render check | 다음 스킬 HTML 갱신 때 첫 viewport가 텍스트 설명보다 조작 가능한 도표와 상태 변화를 먼저 보여주고 card readable width가 유지되는지 확인한다. |
| 2026-05-20 | `skill-update` / workflow source registry | Source registry drift guards added | `active -> active` | 단일 source registry 원칙이 문서 설명에만 머무르지 않도록 workflow usage map에 `source id` 컬럼을 추가하고, `scripts/validate-source-registry.ts`로 source id 존재 여부와 source URL/checked date 중복을 검사하게 했다. `plugins/project-workflow` canonical bundled skill 계약은 `plugins/project-workflow/scripts/validate-project-workflow.ts`로 별도 검증한다. | source registry validator, project-workflow plugin validator, skill-update validator, feature-workflow validator, repo validator, agent eval runner | 다음 workflow primitive 업데이트는 먼저 registry source ledger를 갱신하고, usage map에는 source id와 handoff만 남긴다. |
| 2026-05-20 | all skills | Skill HTML source-evidence refresh applied | `active -> active` | 새 `skill-to-html` 기준을 기존 `skills/*/skill.html`에 적용해 핵심 계약, 원문 근거, 실제 확인, 작업 흐름도, 파일 관계도, 금지/허용, PC desktop 검증 표를 공통 구조로 맞췄다. `show-skills` catalog marker는 생성 스크립트로 다시 동기화했고, 전용 validator가 요구하는 workflow/module/source 문구는 각 스킬 HTML에 별도 계약으로 보강했다. | all skill validators, show-skills catalog check, common HTML validator, Korean Markdown validator, plugin validator, repo validator, agent eval runner, Chrome headless PC browser render check for 17 HTML files | 다음 개별 스킬 변경 때는 이 공통 구조 위에서 해당 스킬 전용 도표만 추가로 보강한다. |
| 2026-05-20 | `skill-update` / workflow source registry | Single source registry clarified | `active -> active` | workflow primitive의 source URL, checked date, upstream version/commit, local 채택 판단이 `project-workflow`와 `feature-workflow` usage map에 중복되지 않도록 `docs/update-source-registry.md`를 단일 source registry로 승격했다. 각 workflow의 `upstream-dependency-map.md`는 source id 사용 순서와 handoff만 담는 usage map으로 낮췄다. | skill-update validator, project-workflow validator, feature-workflow validator, plugin validator, skill HTML validator, repo validator | 다음 dependency update sweep에서는 source ledger를 먼저 registry에 쓰고, workflow map에는 URL과 checked date를 복사하지 않는다. |
| 2026-05-20 | `project-workflow` | Phase runner step metadata normalized | `active -> active` | GitHub fresh clone 기반 `skill-test` cycle에서 실제 생성된 phase `index.json`이 `id`/`title`/`file` 형식인데 `execute-phase.ts`가 `step`/`name`만 읽어 dry-run prompt에 `Step undefined: undefined`를 출력하는 결함을 발견했다. runner가 두 형식을 모두 정상화하도록 고치고, 테스트 방법 문서에 `Step undefined`를 실패 조건으로 추가했다. | execute-phase validator, project-workflow validator, plugin validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, fresh clone regression | 다음 cycle에서는 GitHub 최신본으로 clone한 runner가 실제 generated `index.json`에서 `Step step1: ...`처럼 명확한 header를 출력하는지 확인한다. |
| 2026-05-20 | `project-workflow` | Grill with Docs default gate added | `active -> active` | Matt Pocock의 최신 `grill-with-docs` 흐름을 확인해 project setup 기본 domain gate를 `grill-me`에서 `grill-with-docs`로 대체했다. raw idea 첫 응답은 `CONTEXT.md`, `CONTEXT-MAP.md`, ADR 후보를 고려하는 docs-aware interview를 먼저 두고, `office-hours`, Superpowers `brainstorming` setup gap check 뒤에만 디자인, 구조, PRD, 이슈로 넘어간다. 반복 검증을 위해 `docs/project-workflow-test-method.md`에 GitHub fresh clone cycle 절차와 history 기록 형식을 추가했다. | project-workflow validator, agent eval runner, skill HTML validator, repo validator, fresh clone cycle | 다음 project-workflow 변경은 같은 테스트 방법으로 cycle 번호를 올려 실제 첫 응답과 실제 파일 생성을 다시 확인한다. |
| 2026-05-20 | `project-workflow` | Superpowers setup gap check ordered | `active -> active` | fresh clone 테스트 계획 검토 중 실제 첫 응답 fixture가 `grill-me`와 `office-hours` 뒤 Superpowers `brainstorming`을 `deferred`로 미루고 바로 디자인 gate로 넘어갈 수 있음을 확인했다. raw idea 첫 응답은 `grill-me`/`grill-with-docs` -> `office-hours` -> Superpowers `brainstorming` setup gap check 순서로 질문하고, `design.md`, `project-structure`, PRD, issue backlog는 그 뒤까지 `deferred` 또는 `not created yet`로 남기도록 계약과 eval을 보강했다. | project-workflow validator, agent eval runner, skill HTML validator, repo validator | GitHub fresh clone 기반 `skill-test` cycle에서 실제 Codex 첫 응답이 같은 순서를 내는지 확인하고, 실패하면 다음 cycle에서 계약을 다시 좁힌다. |
| 2026-05-20 | `project-workflow` | First-response gate clarified | `active -> active` | 4개 실제 서비스 시나리오를 병렬로 시뮬레이션하니 질문 gate는 잘 작동했지만, `selected`가 즉시 실행인지 나중 실행인지 애매하고 첫 응답에서 산출물이 이미 만들어진 것처럼 보일 수 있었다. Primitive 상태에 `deferred`를 추가하고, raw idea 첫 응답은 domain/product 질문을 먼저 하며 artifact path를 target/proposed/not created yet로 표시하도록 했다. | project-workflow validator, plugin validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner, PC browser render check | 실제 프로젝트 bootstrap에서는 사용자가 질문에 답한 뒤에만 `CONTEXT.md`, ADR, PRD, `design.md`, workflow-state를 생성하는지 계속 확인한다. |
| 2026-05-20 | `project-workflow` | Agent-neutral phase runner added | `active -> active` | `harness_framework`의 `execute.py`는 Python/Claude 전용 실행 엔진으로 복사하지 않고, phase/step 상태와 자기완결 handoff 구조만 차용했다. 선택 실행 도구는 TypeScript `plugins/project-workflow/scripts/execute-phase.ts`로 추가했고 Codex/Claude 모두 `--agent-bin`/`--agent-arg`와 stdin boundary로 받을 수 있게 했다. | project-workflow validator, plugin validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | 실제 target repo에서 runner를 켤 때는 먼저 dry-run prompt를 확인하고, 실행/commit/push는 별도 사용자 요청과 `atomic-committer` 경계로 분리한다. |
| 2026-05-20 | `skill-update` | Dependency update sweep added | `active -> active` | 외부 dependency 전체 갱신 요청에서 `caveman`, `context-mode`, `code-review-graph` 같은 `.gitmodules` vendored plugin만 보거나, `project-workflow`/`feature-workflow`의 Matt Pocock, GStack, Superpowers primitive만 보는 누락을 막기 위해 하나의 sweep 범위로 묶었다. | skill-update validator, plugin validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | 실제 upstream bump 실행 시 release/changelog 확인, submodule commit/tag, plugin manifest, registry workflow primitive lane이 과도하게 무겁지 않은지 점검한다. |
| 2026-05-20 | `project-workflow` | Project workflow plugin boundary added | `active -> active` | `project-workflow`는 초기 셋팅 orchestration이라 `plugins/project-workflow` repo-owned plugin으로 canonical bundle을 만들었다. `feature-workflow`는 초기 셋팅 이후 반복 개발 skill이라 plugin core에서 제외했고, `agent-eval-harness`는 setup 실행이 아니라 companion 검증 스킬로 분리했다. | workflow validators, plugin validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | plugin-bundled skill 계약을 validator로 계속 강화한다. |
| 2026-05-19 | `project-workflow` | Upstream primitive invocation contract added | `active -> active` | `grill-me`, `grill-with-docs`, `office-hours`, 조건부 Superpowers `brainstorming`/`writing-plans`를 단순 출처 라벨이 아니라 실제 호출 우선 primitive로 다루도록 바꿨다. 현재 runtime에 원본 skill/plugin/command surface가 없으면 `fallback`으로 표시하고 domain/product interview 질문을 먼저 수행한 뒤 PRD, issue backlog, architecture handoff로 넘어간다. | project-workflow validator, Korean Markdown validator, skill HTML validator, repo validator, agent eval runner | 원본 package나 Codex/Claude plugin 호출 방식이 바뀌면 `references/upstream-dependency-map.md`와 fallback 질문 gate를 함께 갱신한다. |
| 2026-05-19 | `spec-workflow` -> `feature-workflow` | Skill renamed | `active -> active` | 사용자가 기존 이름보다 `feature-workflow`가 더 직관적이라고 판단해, 반복 구현 스킬의 공개 이름과 경로를 `feature-workflow`로 바꿨다. 범위는 feature뿐 아니라 기존 PRD, issue, spec, bug report, acceptance criteria, ADR, `design.md` 기반 vertical slice 구현까지 포함한다고 설명을 보강했다. | feature-workflow validator, show-skills validator, common skill validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, PC browser render check | 외부 프로젝트가 아직 `spec-workflow` snippet을 링크하고 있으면 `feature-workflow`로 교체한다. |
| 2026-05-18 | `project-workflow` / `feature-workflow` | TypeScript ESM-only policy added | `active -> active` | `project-structure`의 TypeScript ESM-only 기준을 workflow suite에도 적용했다. 초기 셋팅은 ESM only와 CommonJS 금지를 architecture constraint로 남기고, 반복 구현은 TypeScript production edit 전에 `type: "module"`, ESM `tsconfig`, `import`/`export`, CommonJS blocker를 확인하도록 했다. | project-workflow validator, feature-workflow validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check | 기존 CommonJS target repo에서 migration boundary와 blocker 문구가 실제 구현 흐름을 과하게 막지 않는지 점검한다. |
| 2026-05-18 | `project-structure` | TypeScript ESM-only policy added | `active -> active` | 사용자가 TypeScript를 사용할 때 ESM을 사용하고 CommonJS를 절대 사용하지 않도록 요청해, `project-structure`의 기본 구조 정책에 `package.json` `type: "module"`, ESM `tsconfig`, `import`/`export` 사용, `require`/`module.exports`/`.cjs`/`.cts` 금지를 추가했다. | project-structure validator, show-skills catalog check, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check | 실제 scaffold에서 외부 tool config가 CommonJS를 요구하는 것처럼 보이면 fallback을 만들지 말고 ESM 지원 경로나 사용자 승인 경계를 확인한다. |
| 2026-05-18 | all skills | Skill HTML guides regenerated | `active -> active` | `skill-to-html`의 새 기준을 적용해 모든 `skills/*/skill.html`을 한국어 문장 우선, 도표 중심, PC desktop 기준 템플릿으로 다시 만들었다. `show-skills`의 catalog 생성 스크립트도 영어 설명어가 되살아나지 않도록 한국어 요약으로 맞췄다. | all skill validators, show-skills catalog check, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check for 17 HTML files | 개별 스킬마다 더 풍부한 전용 도표가 필요하면 이 공통 템플릿 위에서 해당 스킬만 보강한다. |
| 2026-05-18 | `skill-to-html` | Korean visible copy density guard added | `active -> active` | `skill.html` 생성 시 `Workflow suite`, `setup`, `domain language`, `source-labeled primitive` 같은 영어 설명어가 화면에 과하게 남아 사용자가 이해하기 어렵다는 피드백이 있어, 화면 설명을 한국어 문장 우선으로 쓰고 영어 나열을 금지하는 기준과 예시를 추가했다. 현재 `project-workflow/skill.html`도 같은 기준으로 정리했다. | skill-to-html validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check | 새 HTML을 만들 때 정확한 파일명, 명령, 제품명, `TDD`, `QA`, `API`, `MCP`, `PRD` 외의 설명어가 다시 영어 목록으로 쌓이는지 확인한다. |
| 2026-05-18 | `skill-to-html` | PC-only rendering scope added | `active -> active` | 사용자가 `skill-to-html`의 산출물을 mobile/tablet까지 신경 쓰지 말고 PC 버전만 보도록 요청해, viewport 기준을 PC desktop으로 고정하고 mobile/tablet layout, responsive breakpoint, touch interaction을 비대상으로 명시했다. | skill-to-html validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, PC browser render check | 실제 skill HTML 변경에서 mobile/tablet QA를 반복하지 않고 PC desktop evidence만 보고하는지 확인한다. |
| 2026-05-18 | `project-workflow` | Korean-first setup documents added | `active -> active` | 초기 셋팅 때 생성하는 `CONTEXT.md`, ADR, PRD, issue backlog, `design.md`, setup validation, `workflow-state.md`, `work-claims.md`가 사용자의 별도 언어 지정 없이 영어로 흘러가는 것을 막기 위해 한국어 우선 문서 규칙과 원문 보존 예외를 추가했다. | project-workflow validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | target project가 이미 영문 문서 규칙을 갖는 경우 `workflow-state.md`에 override 이유가 남는지 실제 setup에서 확인한다. |
| 2026-05-18 | `skill-to-html` / `skill-update` | Skill HTML render integrity guard added | `active -> active` | `skill-update/skill.html`에서 원본 확인 의사결정 SVG arrow가 빈 공간을 가리키고, 변경 범위 표가 2열 layout 안에서 좁게 깨지는 문제가 확인되어 HTML을 재배치했다. `skill-to-html` 기준과 common HTML validator에는 wide scope table 2열 배치와 dangling SVG arrow endpoint 검사를 추가했다. | skill-to-html validator, skill-update validator, common HTML validator, browser render check, Korean Markdown validator, repo validator, agent eval runner | 정적 SVG endpoint 검사는 `path marker-end` 중심이므로, future HTML에서 `line`이나 inherited marker가 깨지면 검사 범위를 확장한다. |
| 2026-05-18 | repo plugin catalog / `caveman` | Codex SessionStart hook added | `active reference -> active reference` | Caveman plugin이 Codex 세션 시작 때 짧은 reminder만 주입하면 실제 응답 규칙이 약해질 수 있어, Codex plugin manifest에 `hooks.json`을 연결하고 `hooks/codex/sessionstart.mjs`가 `plugins/caveman/plugins/caveman/skills/caveman/SKILL.md` 본문을 읽어 `CAVEMAN MODE ACTIVE` 컨텍스트로 출력하도록 했다. 이 repo의 `.codex/config.toml`도 같은 script를 호출한다. | plugin validator, repo hook smoke test | submodule commit을 upstream 또는 접근 가능한 fork에 publish하지 않으면 parent repo에서 해당 gitlink를 원격 clone이 가져올 수 없다. |
| 2026-05-18 | `warp-automation` | Skill removed | `active -> none` | 사용자 요청으로 current skill inventory에서 제거했다. `skills/warp-automation`, project snippet, README/AGENTS 연결, static catalog, show-skills catalog, eval case를 삭제했다. | skill-update workflow, show-skills validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | Warp response-title fallback이 다시 필요하면 새 요청에서 더 작은 범위의 스킬로 재설계한다. |
| 2026-05-18 | `skill-update` / repo plugin catalog | Plugin update lane and drift guard hardened | `active -> active` | `skill-update`가 plugin update를 incidental하게 수행하지 않되, 사용자가 plugin update를 명시하면 `.gitmodules` 기반 plugin update lane으로 계속 진행하도록 정리했다. `scripts/validate-plugins.ts`는 `.gitmodules`를 직접 파싱해 `docs/plugin-catalog.md`와 `Plugin update list`의 path/URL drift를 잡고, 새 plugin submodule 추가 시 catalog/list 누락을 실패로 처리한다. | skill-update validator, plugin validator, repo validator, HTML validator, Korean Markdown validator, agent eval runner | plugin-specific manifest 검사는 현재 known plugin별로 남아 있으므로 새 plugin을 추가하면 generic drift guard 통과 후 필요한 manifest profile을 validator에 추가한다. |
| 2026-05-18 | `skill-update` / repo plugin catalog | Plugin update list added | `active -> active` | `.gitmodules`는 machine-readable canonical source지만 plugin bump는 `skill-update` 범위 밖이라, `docs/update-source-registry.md`에 사람이 읽는 `Plugin update list`를 추가했다. `skill-update`는 submodule, MCP config, plugin manifest, bundled plugin skill 업데이트가 필요하지만 사용자가 plugin update를 명시하지 않았으면 이 목록의 path, upstream URL, 점검 파일을 보고 별도 plugin update 요청이 필요하다고 멈춘다. | skill-update validator, plugin validator, repo validator | 새 plugin submodule을 추가하면 `.gitmodules`, `docs/plugin-catalog.md`, `Plugin update list`, `scripts/validate-plugins.ts`, history를 함께 갱신한다. |
| 2026-05-18 | repo plugin catalog | `caveman` plugin reference added | `none -> active reference` | `JuliusBrussee/caveman`은 Claude/Codex/Gemini 등 여러 agent에 terse output, token-compression mode, review/commit/stats/compress skills, Cavecrew subagents를 제공하는 upstream skill/plugin package이므로 `plugins/caveman` submodule로 `v1.8.2`에 고정했다. 이 repo의 top-level `skills/`로 풀지 않고 `.gitmodules`, `docs/plugin-catalog.md`, `scripts/validate-plugins.ts`로 package metadata, Claude plugin manifest, bundled skills 경계를 검증한다. | web-research source check, plugin validator, repo validator | upstream 업데이트가 필요하면 `.gitmodules`의 `plugins/caveman` submodule tag를 명시적으로 올리고 `package.json`, `.claude-plugin/plugin.json`, bundled skill 경로를 다시 확인한다. |
| 2026-05-18 | `warp-automation` | Response-title fallback simplified | `active -> active` | 실제 Warp UI가 assistant 응답 첫 줄을 title 후보로 선택하는 동작에 맞춰 스킬 기본 경로를 응답 형식 하나로 줄였다. terminal session 탐색, title sequence, 질문 파일, Warp 설정 파일, prompt submit hook은 기본 경로에서 제거하고 별도 요청 없이는 실행하거나 설명하지 않도록 했다. | warp-automation validator, common skill validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner | 이미 외부 프로젝트가 이전 terminal/config workflow 문구를 복사했다면 새 `project-snippets/warp-automation.md`로 교체한다. |
| 2026-05-18 | `warp-session-automation` -> `warp-automation` | Skill renamed | `active -> active` | 사용자 요청에 따라 Warp 전용 스킬의 공개 slug를 더 짧은 `warp-automation`으로 정리했다. folder, snippet, metadata, validator, hook wrapper, README/AGENTS/docs/eval 참조, `skill.html`, show-skills catalog를 새 이름으로 맞추고 기존 Warp 전용 동작 계약은 유지했다. | warp-automation validator, common skill validator, common HTML validator, Korean Markdown validator, plugin validator, repo validator, agent eval runner, show-skills catalog check | 삭제 전에는 기존 프로젝트에서 이전 slug나 이전 folder를 직접 참조하면 `$warp-automation`과 해당 skill entry로 교체하도록 안내했다. |
| 2026-05-18 | `terminal-session-automation` -> `warp-session-automation` | Skill renamed and narrowed to Warp | `active -> active` | 현재 Warp에서 OSC title readback이 TTY별로 다르게 동작해, cmux/generic terminal fallback을 제거하고 target TTY별 `matched:true`/`verified:true` 확인, `WARP_DISABLE_AUTO_TITLE=true` start command가 들어간 Tab Config/Launch Configuration, `.scratch/warp-session/current-question.md` watcher pane 적용 경로를 기본으로 바꿨다. folder, snippet, metadata, validator, hook wrapper, docs, eval, `skill.html`, show-skills catalog를 새 이름과 Warp 전용 계약에 맞췄다. | warp-session-automation validator, common skill validator, common HTML validator, Korean Markdown validator, repo validator, agent eval runner, show-skills catalog check | 현재 session에서 새 Warp tab/window를 여는 `open warp://...`는 사용자 명시 승인 후 실행한다. |
| 2026-05-18 | `skill-update` | Update source registry added | `active -> active` | 업데이트 후보를 `references`, history, plugin catalog, `.gitmodules`에서 산발적으로 찾지 않도록 `docs/update-source-registry.md`를 단일 진입점으로 추가했다. Vendored plugin/submodule 목록은 `.gitmodules`를 canonical source로 보고, workflow provenance-only primitive는 workflow usage map과 분리해 관리한다. | skill-update validator, plugin validator, repo validator, agent eval runner | plugin catalog가 `.gitmodules`와 drift 나면 validator가 바로 잡도록 hardcoded plugin 목록을 줄일지 검토한다. |
| 2026-05-18 | `web-research` | Explicit parallel delegation contract added | `critical -> critical` | 상위 runtime 정책이 sub-agent 사용에 explicit delegation request를 요구할 수 있으므로, `web-research` 또는 alias 호출 자체가 research portion에 대한 explicit parallel sub-agent fan-out, delegation, parallel agent work 요청이라고 AGENTS, snippets, SKILL, reference, metadata, HTML, eval에 명시했다. | web-research validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 Codex 세션에서 `web-research` alias 요청이 별도 병렬 요청 문장 없이도 `spawn_agent` 사용 조건을 만족하는지 확인한다. |
| 2026-05-18 | `skill-update` | Original source refresh lane hardened | `active -> active` | 사용자가 `skill-update`를 호출하면 별도 지시가 없어도 대상 스킬의 original/upstream provenance preflight를 실행하고, 원본이 있거나 의심되면 `web-research`로 공식 source, release/changelog, version/commit을 확인한 뒤 `adopt`/`adapt`/`reject`/`defer` 판단을 남기고 local package에 반영하도록 강화했다. | skill-update validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 원본 source가 있는 스킬에서 전체 복사가 아니라 이 repo의 cross-agent 계약에 맞는 delta만 반영되는지 점검한다. |
| 2026-05-18 | `skill-update` | Upstream-first update workflow added | `active -> active` | 원본 skill이나 upstream primitive가 있는 shared skill을 local-only 추측으로 수정하지 않도록, local provenance 확인 후 `web-research`로 공식 source를 검증하고 runtime이 허용하면 병렬 source lane으로 후보와 변경 이력을 비교한 뒤 원본 delta를 local package에 반영하는 계약을 추가했다. | skill-update validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 원본 수정 권한이 없을 때 source ledger와 checked date가 충분히 남는지 점검한다. |
| 2026-05-18 | repo plugin catalog | `code-review-graph` plugin reference added | `none -> active reference` | `code-review-graph`는 Python package, MCP config, bundled upstream skills를 가진 code review graph 도구이므로 `plugins/code-review-graph` submodule로 `v2.3.3`에 고정했다. 이 repo의 top-level `skills/`로 풀지 않고 `docs/plugin-catalog.md`와 `scripts/validate-plugins.ts`로 package metadata, MCP command, bundled skills 경계를 검증한다. | plugin validator, repo validator | upstream 업데이트가 필요하면 submodule tag를 명시적으로 올리고 `pyproject.toml`, `.mcp.json`, bundled skill 경로를 다시 확인한다. |
| 2026-05-18 | repo plugin catalog | `context-mode` plugin reference added | `none -> active reference` | `context-mode`는 스킬 단독 파일이 아니라 `.codex-plugin/plugin.json`, MCP config, hooks, bundled skills를 가진 upstream plugin이므로 `plugins/context-mode` submodule로 `v1.0.136`에 고정했다. 이 repo의 top-level `skills/`로 풀지 않고 `docs/plugin-catalog.md`와 `scripts/validate-plugins.ts`로 plugin 경계를 검증한다. | plugin validator, repo validator | upstream 업데이트가 필요하면 submodule tag를 명시적으로 올리고 manifest/MCP entrypoint를 다시 확인한다. |
| 2026-05-18 | `project-workflow` / `feature-workflow` / `agent-eval-harness` | Parallel work claims added | `active -> active` | 멀티 session, 멀티 agent, 멀티 worktree가 같은 파일을 동시에 수정하는 위험을 줄이기 위해 setup 단계에는 `.scratch/<slug>/work-claims.md` lane ownership 계약을 추가하고, implementation 단계에는 claimed write set preflight와 overlap block 규칙을 추가했다. shared/hotspot file은 integration owner가 맡도록 정리하고, eval harness에는 lane ownership과 overlap block seed를 추가했다. | project-workflow validator, feature-workflow validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 target project에서 stale claim 정리, worktree 이름, integration owner handoff가 너무 무겁지 않은지 점검한다. |
| 2026-05-18 | `course-evaluator` | Skill removed | `active -> none` | 사용자 요청으로 current skill inventory에서 제거했다. `skills/course-evaluator`, project snippet, catalog, README/AGENTS 연결, eval case를 삭제했다. | all skill validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | 관련 채점 작업은 필요 시 새 요청에서 별도 스킬로 다시 설계한다. |
| 2026-05-15 | `course-evaluator` | Skill added | `none -> active` | 반복되는 코드잇 평가 작업에서 학생별 Markdown 파일, embedded newline CSV 파싱, 합쇼체, 러프한 채점, 거의 모범 답안 수준의 개별 코멘트, 반복 코멘트 방지 기준이 반복되어 전용 스킬로 분리했다. | course-evaluator validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | 실제 새 평가 CSV에서 학생 수와 파일 수, 반복 문장 검출 기준이 충분한지 점검한다. |
| 2026-05-14 | `cmux-automation` -> `warp-session-automation` | Skill renamed and broadened | `active -> active` | 기존 cmux 전용 prompt pinning 스킬을 Warp와 generic terminal session까지 다루는 host-aware 자동화 스킬로 넓혔다. 파일명, snippet, OpenAI metadata, docs, eval, validator, hook wrapper, `skill.html`, show-skills catalog를 새 이름에 맞췄고, prompt hook은 cmux에서는 tab/status, Warp에서는 OSC title sequence로 degrade한다. | warp-session-automation validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | hook command 이름이 바뀌었으므로 Codex `/hooks` 재승인이 필요할 수 있다. |
| 2026-05-14 | `browser-qa` | Browser cleanup required | `active -> active` | QA가 끝난 뒤 Playwright/Chrome tab과 직접 시작한 local HTTP/dev server를 닫는 cleanup step을 `SKILL.md`, reference, snippets, metadata, catalog, visual guide에 반영했다. | browser-qa validator, common HTML validator, repo validator, browser render check | 사용자가 계속 열어 둘 URL을 명시한 경우만 server/browser 유지 사유를 final report에 남긴다. |
| 2026-05-14 | `feature-workflow` | TDD hook contract documented | `active -> active` | 사용자가 target code repo에서는 코드 작성 시 TDD를 강제하고 싶지만 이 shared skills repo는 코드 제품 repo가 아니므로 hook 구현이 아니라 문서 계약만 원한다고 정정했다. 이에 따라 `feature-workflow`에 target code repo 전용 project-local TDD hook 계약을 문서화하고, RED evidence, characterization evidence, explicit TDD exception 중 하나가 production code edit 전에 필요하다고 정리했다. 이 repo에는 TDD hook을 설치하거나 실행하지 않는다는 금지 조건을 추가했다. | feature-workflow validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 target code repo에 적용할 때는 project-local hook을 warn mode로 먼저 검증한 뒤 block mode로 올린다. |
| 2026-05-14 | `project-workflow` / `feature-workflow` | Workflow suite boundary hardened | `active -> active` | 구현 fallback, CLI/no-browser evidence, completion/ship mapping, document sync artifact hygiene eval을 `feature-workflow`로 이동하고, `project-workflow`에는 setup gate와 handoff cache를 남겼다. 두 스킬에 `workflow-state.md` cache 계약을 추가하고, `feature-workflow`에는 실패/누락 기반 `agent-eval-harness` seed 규칙을 추가했다. Eval `scope`도 `project-workflow`와 `feature-workflow`로 분리했다. | project-workflow validator, feature-workflow validator, common HTML validator, repo validator, agent eval runner | 실제 프로젝트에서 `workflow-state.md`가 과도한 source of truth가 되지 않고 authority docs의 색인 역할만 하는지 점검한다. |
| 2026-05-14 | `workflow` -> `project-workflow` / `feature-workflow` | Skill split | `active -> active` | 기존 `workflow`가 프로젝트 초기 셋팅과 TDD 기반 반복 구현을 함께 맡아 범위가 과도해졌기 때문에, 초기 domain/product/ADR/design/PRD/issue setup은 `project-workflow`로 좁히고, 기존 spec/issue 기반 구현 loop는 `feature-workflow`로 분리했다. | project-workflow validator, feature-workflow validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 기존 프로젝트 snippet에서 `workflow` 링크를 쓰는 곳은 새 setup/implementation 경계에 맞춰 `project-workflow` 또는 `feature-workflow`로 교체한다. |
| 2026-05-14 | `workflow` | Source-labeled primitive provenance added | `active -> active` | 사용자 workflow가 Matt Pocock/GStack/Superpowers 전체 체인을 복제하는 것이 아니라 영상과 개인 custom 규칙에서 필요한 primitive만 선별하는 구조임을 명확히 하기 위해, `grill-me`, `office-hours`, `writing-plans`, `subagent-driven-development` 같은 이름에 source package와 exact skill/plugin name을 붙이고 upstream 업데이트 규칙을 provenance ledger, snippets, HTML, eval fixture에 연결했다. | workflow validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | upstream skill/plugin 이름이나 산출물 경로가 바뀌면 full copy가 아니라 adopted role, handoff, artifact, validator/eval boundary만 갱신한다. |
| 2026-05-13 | `transcript-polisher` | Source/output and polish safeguards added | `active -> active` | 실제 긴 전사본 반복 작업에서 검토본이 요약본처럼 과도하게 압축되거나 source root에 패치가 잘못 적용될 수 있는 위험을 확인해, source/output root 결정, `polish` output mode, 확정/불확실 용어 장부, output root 패치 적용 안전 규칙을 추가했다. | transcript-polisher validator, Korean Markdown validator, common HTML validator, repo validator | 실제 프로젝트별 기본 root가 `transcripts_raw`/`transcripts_reviewed`가 아닐 때 질문 흐름이 충분히 짧은지 확인한다. |
| 2026-05-13 | `transcript-polisher` | Goal/subagent policy clarified | `active -> active` | Claude Code `/goal`은 자가 개선 스킬이 아니라 완료 조건과 대화상 증거를 별도 평가자가 감사하는 루프라고 명시하고, subagent 분할은 사용자가 요청했거나 활성 runtime 정책이 허용할 때만 쓰도록 제한했다. | transcript-polisher validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | 실제 긴 전사본 검토에서 목표 조건이 증거 기반으로 충분히 좁은지 확인한다. |
| 2026-05-13 | `workflow` / `agent-improvement-loop` / `agent-eval-harness` / `pull-request` | Goal condition recipes added | `active -> active` | 이 repo에서 `/goal` 언급은 Claude Code의 `/goal` 기능을 기준으로 한다고 명시했다. 긴 workflow/backlog/PR 작업에는 measurable end state, check evidence, constraints, turn/time bound가 있는 Claude Code goal condition을 제안하고 다른 runtime에서는 completion checklist로 쓰도록 정리했다. Eval harness에는 goal condition quality case를 추가했다. | affected skill validators, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 Claude Code 프로젝트에서 `/goal` 조건이 과도하게 장기 실행되지 않는지 turn/time bound를 점검한다. |
| 2026-05-13 | `transcript-polisher` | Skill added | `none -> active` | 전사본과 강의 대본을 코드 치환 없이 직접 읽어 문맥 흐름, 용어 일관성, 문단 연결을 다듬고, 문단 단위 subagent 분할과 Claude Code `/goal`식 완료 조건 루프를 공유 스킬로 재사용할 수 있게 추가했다. | base skill validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | 실제 긴 전사본 검토에서 subagent 단위 분할과 최종 통합 품질을 점검한다. |
| 2026-05-13 | `web-research` | Web search alias added | `critical -> critical` | 사용자가 `web-search`, `web search`, `웹서치`, `웹 검색`이라고 말해도 repo의 실제 스킬명인 `web-research`로 해석하도록 SKILL, reference, metadata, snippets, AGENTS, README, catalog, eval에 alias 규칙을 추가했다. | web-research validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 새 프로젝트 instruction에 snippet을 붙일 때 alias 문구가 누락되지 않는지 확인한다. |
| 2026-05-13 | `pull-request` | Repo PR convention added | `active -> active` | 이 repo에 PR을 남길 때 한국어 title/body, summary/scope/validation/risk/linked issue 순서, 스킬 변경 시 `skills/**/*.md` 한국어 우선 규칙과 Korean Markdown validator를 명시하도록 `.github/pull_request_template.md`, pull-request 스킬, snippets, docs, eval을 연결했다. | pull-request validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 PR 생성 시 template이 너무 장황하지 않은지 확인한다. |
| 2026-05-13 | `pull-request` | Skill added | `none -> active` | GitHub 공식 문서와 GitHub CLI `gh pr create` 기준을 확인해 PR 생성/업데이트/초안/reviewer/label/base/head/body/issue link 관리를 `atomic-committer`와 `code-review`에서 분리한 전용 스킬로 추가했다. | pull-request validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner | 실제 PR 생성 요청에서 `gh pr create --dry-run`의 push 가능성, fork prompt, merge/close/delete guardrail이 충분한지 점검한다. |
| 2026-05-13 | `web-research` | Automatic multi-agent research default added | `critical -> critical` | 사용자가 별도로 요청하지 않아도 web-research가 활성화되면 runtime이 delegation을 허용하는 한 parallel sub-agent fan-out을 자동으로 사용하고, 단일 에이전트 조사는 사용자가 명시하거나 private data/runtime policy/tiny official quick check 같은 예외에서만 쓰도록 규칙을 강화했다. downstream artifact 입력 조사는 verified search로 분류하고, 단일 에이전트 예외를 쓰면 skip reason을 남긴다. | web-research validator, Korean Markdown validator, common HTML validator, repo validator, agent eval runner, show-skills catalog check | 실제 리서치에서 하위 agent 분기 수가 과도하지 않은지 확인한다. |
| 2026-05-13 | repo | Korean-first skill Markdown rule added | `active process -> active process` | 사용자가 `skill.html`과 Markdown 문서를 직접 대조할 수 있도록 `skills/**/*.md`를 한국어 우선 문서로 전환하고, `SKILL.md`와 `references/*.md`의 일반 설명/절차/표 라벨은 한국어로 쓰되 코드 식별자와 명령, 파일 경로, 제품명만 원문 표기를 허용하는 repo 규칙과 validator를 추가했다. | Korean Markdown validator, all skill validators, show-skills catalog regeneration, repo validator | 새 스킬이나 reference를 추가할 때 영어-only Markdown 제목과 본문이 다시 들어오지 않는지 `validate-korean-markdown.ts`로 확인한다. |
| 2026-05-12 | `show-skills` | Skill added | `none -> active` | 스킬 수가 늘어나 직접 파일을 클릭하지 않아도 현재 스킬 목록을 카테고리별로 보고, 작업에 맞는 primary/companion skill을 추천할 수 있도록 탐색 스킬과 `docs/skill-catalog.md`를 추가했다. | show-skills validator, common HTML validator, repo validator | 새 스킬 추가 시 live listing과 정적 catalog가 함께 최신화되는지 점검한다. |
| 2026-05-12 | `show-skills` | HTML catalog sync automated | `active -> active` | `show-skills/skill.html`의 전체 HTML 카탈로그를 marker 기반 생성 블록으로 바꾸고, `update-html-catalog.ts --check`가 누락된 skill link를 validator에서 실패시키도록 했다. | show-skills validator, common HTML validator, repo validator | 새 스킬 inventory 변경 시 생성 스크립트가 먼저 실행되고 validator가 stale catalog를 막는지 확인한다. |
| 2026-05-11 | `web-research` | Web research method hardened | `active -> critical` | query fan-out, source ledger, evidence scoring, stop rules, eval prompts, static validator 기준을 추가했다. | repo TS validator, skill-specific validator | broad topic eval runner는 필요해지면 추가한다. |
| 2026-05-12 | `web-research` | Parallel research delegation added | `critical -> critical` | deep/reproducible research에서 사용자가 명시적으로 허용한 경우 독립 query lane을 subagent에 병렬 위임하고 main agent가 ledger merge와 최종 판단을 맡도록 규칙을 추가했다. | web-research validator, repo validator | 실제 병렬 리서치에서 subagent lane 분해 품질과 citation merge 품질을 점검한다. |
| 2026-05-12 | `web-research` | Parallel research made default | `critical -> critical` | verified/deep/reproducible web research에서는 runtime이 허용할 때 독립 query lane을 subagent에 병렬 위임하는 것을 기본값으로 바꾸고, quick check/private data/runtime policy 제한은 main-agent fan-out으로 degrade하도록 정리했다. | web-research validator, skill-to-html validator, repo validator | 기본 병렬 실행이 과도한지 실제 검색 작업에서 latency와 citation merge 품질을 점검한다. |
| 2026-05-11 | `skill-to-html` | HTML guide standard hardened | `active -> active` | 단순 문서 분할이 아니라 decision matrix, flowchart, chart, resource map 중심으로 HTML을 만들도록 기준을 정리했다. | repo TS validator, skill-specific validator | 새 skill이 늘면 visual pattern 예시를 보강한다. |
| 2026-05-12 | `skill-to-html` | Skill explanation design standardized | `active -> active` | `skills/skill-to-html/references/visual-guide-standards.md`의 조용한 operational UI 원칙을 반영해 purpose, use/skip, workflow, input/output, resources, validation, misuse guardrails 순서의 공통 설명 문법과 깔끔한 레이아웃 기준을 추가하고 자체 HTML을 기준 예시로 정리했다. | skill-to-html validator, common HTML validator, repo validator, Playwright render check | 새 스킬이 늘 때 같은 설명 문법과 HTML 검증을 적용한다. |
| 2026-05-12 | repo | Full skill audit fixes applied | `active process -> active process` | multi-agent 전수 검토 결과에 따라 hook 자동 쓰기 범위를 편집 tool로 제한하고, show-skills root/frontmatter handling, project-structure layout guidance, docs validation rules, agent metadata validation을 보강했다. | common validators, skill-specific validators, common HTML validator, repo validator, browser render check | 새 untracked skill package files를 커밋할 때 빠뜨리지 않는지 commit 단계에서 확인한다. |
| 2026-05-12 | `skill-to-html` | Korean-first HTML labels added | `active -> active` | 설치 시 빠르게 읽히도록 `skill.html`의 섹션 제목, 도표 라벨, 매트릭스 셀, 금지/허용 문구는 한국어 우선으로 쓰고, 영어는 코드 용어와 정확한 파일명/명령/제품명에만 남기도록 기준과 검증을 보강했다. | skill-to-html validator, common HTML validator, skill-specific validators, repo validator | 새 HTML을 만들 때 영어-only UI 라벨이 다시 생기는지 공통 validator로 확인한다. |
| 2026-05-12 | `karpathy-thinkings` | Skill added | `none -> active` | Karpathy식 코딩 에이전트 사고를 직접 쓰는 구현 스킬을 추가했다. 모호함 노출, 단순함, 외과적 변경, 성공 기준 검증을 핵심 원칙으로 둔다. | repo TS validator, skill-specific validator, repo validator | 실제 코딩 작업에서 질문 과다와 검증 강도의 균형을 점검한다. |
| 2026-05-11 | `skill-update` | Skill added | `none -> active` | 기존 스킬 수정 시 `SKILL.md`, references, validator, `agents/openai.yaml`, `skill.html`, snippets, docs, history를 한 번에 맞추는 상위 유지보수 스킬을 추가했다. | repo TS validator, skill-specific validator, repo validator | 실제 스킬 업데이트 요청에서 package completeness 품질을 점검한다. |
| 2026-05-12 | `skill-update` | Inventory catalog sync rule added | `active -> active` | 스킬 add/remove/rename/archive/restore 작업에서 `show-skills` HTML 카탈로그를 재생성하고 검증하도록 SKILL, checklist, snippet, visual guide, validator를 연결했다. | skill-update validator, show-skills validator, common HTML validator, repo validator | skill inventory가 바뀌는 다음 작업에서 catalog sync 절차가 누락 없이 작동하는지 확인한다. |
| 2026-05-11 | `changeset-committer` | Skill added | `none -> active` | 커밋 요청 시 변경사항을 changeset 단위로 분류하고, 영어 prefix + 한글 메시지로 커밋하며, remote가 있을 때만 push하도록 새 스킬을 추가했다. | repo TS validator, skill-specific validator | 실제 프로젝트 커밋에서 grouping 품질을 점검한다. |
| 2026-05-11 | `atomic-committer` | Skill renamed | `active -> active` | 기존 `changeset-committer` slug와 표시 이름을 Git 관례에 더 가까운 `atomic-committer` / Atomic Committer로 변경했다. | repo TS validator, skill-specific validator, repo validator | none |
| 2026-05-12 | `atomic-committer` | Secret guard hardened | `active -> active` | 커밋 전 candidate diff와 staged diff를 검사하고, 실제값처럼 보이는 cloud/source-control/AI/payment/messaging/package/database credential assignment와 private key material은 사용자 강제 요청이 있어도 커밋하지 않도록 hard block 규칙을 추가했다. | atomic-committer validator, repo validator | 실제 commit 요청에서 placeholder 허용과 live secret hard-block 보고 품질을 점검한다. |
| 2026-05-13 | `atomic-committer` | Gitignore hygiene added | `active -> active` | 커밋 차단/경고 대상이 untracked local artifact, `.env`, credential path, log/cache/tool state처럼 반복적으로 올라가면 안 되는 파일이면 커밋 전에 좁은 `.gitignore` 패턴을 추가하고, 이미 tracked 된 secret은 `.gitignore`로 숨기지 않고 redaction 또는 사용자 승인 `git rm --cached`가 필요하도록 했다. | atomic-committer validator, common HTML validator, repo validator | 실제 커밋 요청에서 broad ignore pattern을 과하게 추가하지 않는지 점검한다. |
| 2026-05-11 | `project-structure` | Skill added | `none -> active` | 프로젝트 종류를 숫자로 고르고 Bun, Turborepo, Next.js, NestJS Fastify, GraphQL, urql, Drizzle, Zod env, Tauri 기본값으로 일관된 구조를 만들도록 새 스킬을 추가했다. | repo TS validator, skill-specific validator, repo validator | 실제 신규 프로젝트 scaffold에서 구조 적합성을 점검한다. |
| 2026-05-11 | `project-structure` | Backend and monorepo structure policy hardened | `active -> active` | app 공통 env/codegen/autogen 경로를 통일하고, API logger/cache provider 경계와 monorepo Redis DB boundary를 `packages/db/src/redis`로 정리했다. | project-structure validator, repo TS validator | 실제 scaffold에서 Redis/cache 경계가 과도하게 app으로 새지 않는지 점검한다. |
| 2026-05-12 | `project-structure` | Structure verification workflow hardened | `active -> active` | numeric menu, desktop env/codegen tree, GraphQL generated artifact contract, Drizzle migration ownership, Panda CSS structure, test surface, auth/security, health/readiness, observability, 최종 tree 검증 reference를 보강했다. | project-structure validator, repo TS validator, repo validator | 실제 구조 생성에서 질문량과 HTML tree 가독성의 균형을 점검한다. |
| 2026-05-12 | `project-structure` | Infrastructure structure options added | `active -> active` | Pulumi AWS, Docker app images, AWS ECR image registry, ECS Fargate 기본 runtime, EC2 Docker host 선택지와 public entrypoint, stack config example, secret name 문서, immutable image tag handoff, smoke check 기준을 numeric menu, reference, visual guide, snippets, validator에 추가했다. | project-structure validator, common HTML validator, repo validator | 실제 scaffold에서 infra가 요청되지 않은 프로젝트에 `infra/`가 과도하게 추가되지 않는지, ECS/EC2가 동시에 active path로 생성되지 않는지 점검한다. |
| 2026-05-12 | `project-structure` | Database provider options added | `active -> active` | MongoDB Atlas, Supabase Postgres, psql/mongosh shell helper, MongoDB collection/index boundary, Supabase service-role server-only 규칙을 DB menu, reference, visual guide, snippets, validator에 추가했다. | project-structure validator, common HTML validator, repo validator | 실제 scaffold에서 MongoDB 선택 시 Drizzle migration을 만들지 않고, Supabase 선택 시 service-role key가 frontend public env로 새지 않는지 점검한다. |
| 2026-05-12 | `project-structure` / `sync-docs` | Folder AGENTS indexes added | `active -> active` | 의미 있는 boundary folder마다 짧은 `AGENTS.md` 목차를 생성하고, 폴더 구조 변경 시 sync-docs가 해당 local map, do-not-change boundary, related skills, validation commands를 갱신하도록 SKILL, references, snippets, docs, validators, visual guides를 연결했다. | project-structure validator, sync-docs validator, workflow validator, common HTML validator, repo validator, browser render check | 실제 scaffold에서 folder-local instruction이 root rule을 복사하거나 충돌하지 않고 목차 역할만 하는지 점검한다. |
| 2026-05-12 | `workflow` | Skill added | `none -> active` | `skill-practice`의 `WORKFLOW.md`, docs, references에서 검증된 front-loaded 프로젝트 진행 순서를 공유 스킬로 분리해 domain language, ADR, PRD, issue, implementation plan, TDD, QA, document sync, ship 단계를 재사용할 수 있게 했다. | workflow validator, common HTML validator, repo validator | 실제 신규 프로젝트와 feature planning에서 질문량과 산출물 생성 범위의 균형을 점검한다. |
| 2026-05-12 | `workflow` | Cross-agent portability researched | `active -> active` | Codex, Claude Code, Cursor, Windsurf, Copilot의 공식 instruction/rule/workflow surface 차이를 확인한 뒤 workflow 스킬에 adapter surface 확인, 공식 문서 checked date 기록, routing/eval 권장 단계를 추가했다. | workflow validator, common HTML validator, repo validator | 실제 프로젝트 연결 시 agent별 adapter snippet이 너무 길어지지 않는지 점검한다. |
| 2026-05-12 | `workflow` | Product and mockup flow clarified | `active -> active` | 원본 workflow의 GStack `office-hours` 제품 검증, Superpowers 구현 `brainstorming`, `design-shotgun`/`design-consultation`식 mockup 선택 흐름을 대조해 세 단계를 서로 대체하지 않도록 SKILL, playbook, HTML, snippets에 명시했다. | workflow validator, common HTML validator, repo validator | 실제 UI 프로젝트에서 2-3개 mock direction 선택 기록이 과도한 산출물로 흐르지 않는지 점검한다. |
| 2026-05-12 | `workflow` | Source skill call names restored | `active -> active` | 원본 `WORKFLOW.md`의 직접 호출 skill 이름이 빠지지 않도록 `setup-matt-pocock-skills`, `grill-me`, `grill-with-docs`, `office-hours`, `plan-ceo-review`, `to-prd`, `to-issues`, `triage`, `brainstorming`, `writing-plans`, `plan-eng-review`, `plan-design-review`, `subagent-driven-development`, `tdd`, `review`, `qa`, `diagnose`, `document-sync`, `improve-codebase-architecture`, `semantic-commits`, `ship` 순서를 SKILL, playbook, HTML, snippets, validator에 명시했다. | workflow validator, common HTML validator, repo validator | 앞으로 workflow를 범용화할 때 원본 skill 호출명을 추상화로 지우지 않도록 검사한다. |
| 2026-05-12 | `workflow` | Dependency preflight added | `active -> active` | 원본 workflow가 Matt Pocock-style, GStack-style, Superpowers-style, design-direction skill family를 조율하는 구조임을 명시하고, 실행 전에 각 의존 묶음을 `available`, `missing`, `fallback`으로 inventory하도록 SKILL, playbook, HTML, snippets, validator를 보강했다. | workflow validator, common HTML validator, repo validator | 실제 프로젝트에서 누락 의존성을 조용히 fallback하지 않고 보고하는지 확인한다. |
| 2026-05-12 | `workflow` | Upstream dependency map added | `active -> active` | `skill-practice/references`와 project-local installed skills를 대조해 Matt Pocock, GStack, Superpowers, design-direction dependency ledger를 `references/upstream-dependency-map.md`로 분리하고, 원본 skill 업데이트 추적, 중복 stage boundary, 방향 우선, mockup direction gate를 validator와 HTML에 반영했다. | workflow validator, common HTML validator, repo validator | 원본 source skill이 바뀔 때 workflow가 전체 내용을 복사하지 않고 orchestration boundary만 갱신하는지 확인한다. |
| 2026-05-12 | `workflow` | Specialist handoffs clarified | `active -> active` | `project-structure`와 `agent-eval-harness`를 workflow에 합치지 않고 별도 스킬로 유지하되, `project-structure`는 domain language와 구체적 architecture 질문 이후 ADR/PRD 전에 호출하고, `agent-eval-harness`는 workflow routing, dependency inventory, `project-structure` timing, PRD settings, mockup, document sync, artifact hygiene seed case를 만드는 검증 handoff로 명시했다. | workflow validator, agent-eval-harness validator, agent eval runner, common HTML validator, repo validator | 신규 프로젝트에서 workflow가 specialist skill을 너무 빨리 호출하지 않는지 실제 case로 점검한다. |
| 2026-05-12 | `workflow` | Self-check blockers resolved | `active -> active` | multi-agent self-check에서 발견된 PRD/ADR gate 우회, architecture mode의 `project-structure` 누락, raw idea eval의 잘못된 `project-structure` 기대, UI mockup gate 누락, 정적 문구 위주 eval 문제를 수정했다. Workflow eval은 saved output fixture를 요구하고, `project-structure`는 raw product/workflow discovery를 `workflow`로 defer하도록 했다. | workflow validator, project-structure validator, agent-eval-harness validator, workflow eval fixtures, agent eval runner, common HTML validator, repo validator | live/trace 기반 eval이 필요해지면 현재 saved-output fixture를 최소 regression seed로 삼는다. |
| 2026-05-12 | `workflow` / `agent-eval-harness` / `project-structure` / `sync-docs` / `code-review` | Existing skills improved instead of adding new skills | `active -> active` | 현재 설치된 스킬을 강화하는 방향이 더 적합하다는 검토 결과에 따라 workflow에 Local Implementation Fallback Lane, Project Setup Verification, Agent Tool And Security Risk Gate를 추가하고, eval runner에 `required_link_count`, `required_file_reference`, `json_schema`를 구현했으며, project-structure/sync-docs/code-review에 tool/setup/security 검토 경계를 분산했다. | affected skill validators, show-skills catalog check, common HTML validator, repo validator, agent eval runner | 실제 프로젝트 셋업/검증에서 새 스킬 추가보다 기존 스킬 개선으로 충분한지 계속 점검한다. |
| 2026-05-12 | `workflow` / `agent-eval-harness` | Scenario lanes added | `active -> active` | 10개 임시 프로젝트 병렬 시뮬레이션 결과에 따라 raw SaaS/service, existing API cleanup, substantial UI, CLI/no-browser, Docker/AWS/Pulumi, MCP/API/file-write, cross-agent setup, completion/ship lane을 workflow playbook에 추가하고, tool gate decision template, completion local-skill mapping, project setup verification, CLI/runtime evidence, 관련 saved-output eval cases를 보강했다. | workflow validator, agent-eval-harness validator, workflow eval fixtures, agent eval runner, common HTML validator, repo validator | 실제 신규 프로젝트에서 scenario lane이 과도한 질문으로 흐르지 않고 earliest missing gate를 잘 고르는지 점검한다. |
| 2026-05-11 | `sync-docs` | Skill added | `none -> active` | 문서끼리 비교해 stale 설명, 누락된 연결, 충돌하는 규칙을 정리하고 source of truth가 불명확하면 사용자에게 묻는 문서 최신화 스킬을 추가했다. | repo TS validator, skill-specific validator, repo validator | 실제 문서 audit에서 질문 분기 품질을 점검한다. |
| 2026-05-12 | `cmux-automation` | Skill added | `none -> active` | cmux hook, Codex prompt pinning, tab/status automation, workspace boards, feed workflows, browser surfaces, cmux CLI 기반 session ergonomics를 project-local로 관리하는 스킬을 추가했다. | cmux-automation validator, common HTML validator, repo validator | 실제 cmux 환경에서 prompt payload와 graceful degrade 동작을 점검한다. |
| 2026-05-12 | repo | Cmux prompt pin hook added | `active process -> active process` | Codex `UserPromptSubmit`에서 cmux tab/status에 현재 prompt를 고정하는 project-local helper hook을 추가했고, 구현 스크립트를 `cmux-automation` 스킬 번들로 옮겨 재사용 가능하게 했다. | cmux sample payload, cmux-automation validator, common HTML validator, repo validator | Codex `/hooks`에서 새 hook command를 승인해야 현재/다음 세션에서 실행될 수 있다. |
| 2026-05-13 | repo | Local hook root guard added | `active process -> active process` | repo-local Codex hook이 다른 repo에서 잘못 실행되지 않도록 hook script가 실행 cwd의 git root와 script repo root를 비교해 mismatch 시 조용히 종료하게 했고, 전역 `hooks.state`는 승인 해시일 뿐 실행 정의가 아니라는 문서를 추가했다. | hook dry-runs, cmux prompt dry-run, common HTML validator, repo validator | hook script가 바뀌었으므로 Codex `/hooks` 재승인이 필요할 수 있다. |
| 2026-05-12 | repo | Prompt hook latency reduced | `active process -> active process` | 질문 제출마다 실행되던 skill HTML 자동 갱신/검증 hook을 `UserPromptSubmit`에서 제거하고, cmux prompt pinning은 `CMUX_SURFACE_ID` 직접 targeting을 우선해 `cmux identify` 호출을 fallback으로 낮췄다. | latency timing, cmux-automation validator, common HTML validator, repo validator | `.codex/config.toml` 변경 후 `/hooks` 재승인이 필요할 수 있다. |
| 2026-05-12 | `cmux-automation` | Compact tab title policy added | `active -> active` | cmux tab label 폭이 좁아 원문이 잘리는 문제를 줄이기 위해 `rename-tab`에는 짧은 질문 제목만 넣고, 전체 질문 원문은 `current-question` status에 보존하도록 prompt pinning 정책과 구현을 바꿨다. | cmux sample payload, cmux-automation validator, common HTML validator, repo validator | `.codex/config.toml`이나 hook script가 바뀐 세션에서는 Codex `/hooks` 재승인 또는 세션 재시작이 필요할 수 있다. |
| 2026-05-12 | `cmux-automation` | Tab title prefix removed | `active -> active` | 좁은 cmux tab label 공간을 더 쓰기 위해 `Q:` prefix를 제거하고 compact title만 `rename-tab`에 넣도록 prompt pinning 구현과 검증을 조정했다. | cmux sample payload, cmux-automation validator, common HTML validator, repo validator | hook script 변경 후 자동 hook에 즉시 반영되지 않으면 `/hooks` 재승인 또는 세션 재시작이 필요할 수 있다. |
| 2026-05-12 | `cmux-automation` | Rule-based tab labels added | `active -> active` | 탭 제목이 질문 앞부분 그대로 들어가는 문제를 줄이기 위해 prompt를 단순 절단하지 않고 `요약 규칙 수정`, `탭 폭 확인`, `스킬 검토` 같은 빠른 규칙 기반 작업 라벨로 변환하도록 개선했다. | cmux sample payload, cmux-automation validator, common HTML validator, repo validator | 규칙에 없는 표현은 여전히 fallback 축약을 쓰므로, 반복되는 표현은 규칙으로 추가한다. |
| 2026-05-12 | `cmux-automation` | Semantic title boundary clarified | `active -> active` | `UserPromptSubmit` hook은 agent가 의미를 이해하기 전 실행되므로 진짜 요약이 아니라 빠른 fallback 라벨만 만들고, 의미 기반 tab title은 agent가 turn 시작 후 `cmux rename-tab`으로 덮어쓰는 2단계 구조로 문서화했다. | cmux-automation validator, common HTML validator, repo validator | 실제 세션에서 agent-written semantic title을 너무 자주 실행하지 않는지 확인한다. |
| 2026-05-12 | `cmux-automation` | Original prompt tab title restored | `active -> active` | cmux tab hover에서 긴 제목 전체를 확인할 수 있으므로 prompt pinning 기본 동작을 요약/규칙 라벨/절단 없이 질문 원문을 tab title과 `current-question` status에 그대로 넣도록 되돌렸다. | cmux sample payload, cmux-automation validator, common HTML validator, repo validator | hook script 변경 후 자동 hook에 즉시 반영되지 않으면 `/hooks` 재승인 또는 세션 재시작이 필요할 수 있다. |
| 2026-05-12 | `agent-improvement-loop` | Skill added | `none -> active` | 남는 토큰을 소모하는 대신 invocation, validation, alignment, quality lane으로 나눠 skill repo와 일반 code repo 모두에서 durable improvement를 만들도록 새 스킬을 추가했다. | repo TS validator, skill-specific validator, common HTML validator, repo validator | 실제 장기 세션 마무리와 repo hardening 요청에서 track 분기와 질문 기준을 점검한다. |
| 2026-05-12 | `agent-improvement-loop` | Quota-aware routing added | `active -> active` | 신뢰 가능한 reset 시간이 30분 이내면 안전한 backlog를 작은 multi-agent batch로 처리하고, 그 외에는 토큰을 상한선으로 두고 staged review를 수행하도록 budget mode를 추가했다. | skill-specific validator, common HTML validator, repo validator | 실제 런타임에서 quota 조회 가능 여부와 spend-down backlog의 안전성을 점검한다. |
| 2026-05-12 | `agent-improvement-loop` | Spend-down consent gate added | `active -> active` | 남은 토큰 소진형 실행은 자동 조건 분기가 아니라 `남은 토큰을 최대한 사용해서 안전한 backlog를 처리할까요? (예/아니오)` 질문에 대한 사용자 답변으로 결정하도록 바꿨다. | skill-specific validator, common HTML validator, repo validator | 실제 사용에서 질문이 과도하게 반복되지 않는지 점검한다. |
| 2026-05-12 | `agent-eval-harness` | Skill added | `none -> active` | 스킬과 agent instruction이 늘어난 뒤 실제 요청에서 올바른 skill routing, workflow, safety boundary, output shape가 유지되는지 작은 repo-local eval harness로 검증할 수 있도록 새 스킬을 추가했다. | base skill validator, agent-eval-harness validator, common HTML validator, repo validator | 실제 프로젝트에 `evals/agent/` 하네스를 세팅할 때 case schema와 runner 기본값을 점검한다. |
| 2026-05-12 | repo | Initial agent eval harness added | `active process -> active process` | 정적 validator가 잡지 못하는 대표 prompt의 skill routing, safety boundary, output contract를 검증하기 위해 `evals/agent/cases/`와 `scripts/run-agent-evals.ts`를 추가했다. | agent eval runner, show-skills validator, common HTML validator, repo validator | 실제 실패 사례가 생기면 live model eval보다 먼저 최소 regression case로 추가한다. |
| 2026-05-12 | `agent-eval-harness` | Web-researched harness hardening applied | `active -> active` | OpenAI, Anthropic, Promptfoo, Braintrust, LangSmith, OWASP, NIST, MCP, Codex/Claude/Copilot/Cursor/Windsurf 문서 검증 결과에 따라 success criteria, typical/edge/adversarial case mix, cross-agent portability, trigger quality, minimum safety case pack, artifact hygiene, model-grader calibration, dated agent-surface assumptions를 스킬과 starter eval cases에 추가했다. | all skill validators, common HTML validator, repo validator, agent eval runner, diff check | 실제 live routing/model-graded eval을 붙일 때 repeat/variance와 artifact redaction 정책을 다시 검증한다. |
| 2026-05-12 | `browser-qa` | Skill package completed | `none -> active` | 기존 `skill-to-html`과 분리해 실제 브라우저 렌더링, console/network, accessibility snapshot, viewport, link, interaction, text overlap 검증을 담당하는 runtime QA 스킬을 추가하고 `skill.html`과 catalog 연결을 완료했다. | browser-qa validator, common HTML validator, repo validator | 실제 frontend/skill.html 작업에서 browser evidence 보고 형식을 점검한다. |
| 2026-05-12 | `code-review` | Skill package completed | `none -> active` | `karpathy-thinkings`의 구현 태도와 분리해 findings-first 리뷰, SRP/SOLID boundary, missing tests, security risk, JS/TS functional collection style 선호를 담당하는 리뷰 스킬을 추가하고 `skill.html`과 catalog 연결을 완료했다. | code-review validator, common HTML validator, repo validator | 실제 PR/diff 리뷰에서 style preference가 과도하게 작동하지 않는지 점검한다. |
| 2026-05-12 | `design-review` | Skill package completed | `none -> active` | 초기 디자인 기준을 portable reference로 정리해 quiet operational UI, restrained color, stable dimensions, 8px radius, no nested cards/orbs/gradients 기준으로 UI를 리뷰하는 스킬을 추가하고 `skill.html`과 catalog 연결을 완료했다. | design-review validator, common HTML validator, repo validator | 실제 UI 리뷰에서 browser QA와 design judgment 경계가 잘 유지되는지 점검한다. |
| 2026-05-12 | `browser-qa` | Intent verification clarified | `active -> active` | HTTP `200 OK`나 파일 로딩만으로 검증을 끝내지 않고, visible content가 사용자 의도와 `SKILL.md` 계약에 맞는지 확인하도록 workflow, reference, visual guide, validator를 보강했다. | browser-qa validator, common HTML validator, repo validator | 실제 `skill.html` QA에서 content-intent mismatch를 findings로 보고하는지 점검한다. |
| 2026-05-12 | `design-review` | Generalized for shared repos | `active -> active` | 특정 앱 화면명과 제품 전용 규칙을 공유 스킬에서 제거하고, 기존 디자인 시스템과 제품 도메인을 우선하는 범용 디자인 리뷰 기준으로 `SKILL.md`, reference, `skill.html`, validator를 정리했다. | design-review validator, common HTML validator, repo validator | 프로젝트 전용 디자인 규칙은 각 프로젝트 instruction이나 override에 둔다. |
| 2026-05-11 | repo | Lifecycle ledger added | `none -> active process` | inspector scratch와 영구 history를 분리하고, skill state를 관리하기 위한 기준을 추가했다. | docs review | 첫 archived skill이 생기면 archive 이동 절차를 실제 사례로 보강한다. |
| 2026-05-11 | repo | Validator scripts moved to TypeScript | `active process -> active process` | Node 22+ 기준에서 validator는 특별한 런타임 제약이 없으면 `.ts`를 기본으로 쓰도록 정했다. | repo validator, skill-specific validators | Codex hook처럼 호환성이 중요한 파일은 `.mjs` 예외를 유지한다. |
| 2026-05-11 | repo | Base validator and hook corrected | `active process -> active process` | Python 직접 실행 문서를 repo-owned TypeScript validator로 바꾸고, Codex hook이 read-only payload의 `SKILL.md` 문자열을 변경으로 오인하지 않게 실제 dirty skill만 본다. | repo TS validator, repo validator | hook review hash는 설정 변경 후 Codex에서 다시 승인될 수 있다. |
| 2026-05-11 | repo | Skill HTML hook auto-run enabled | `active process -> active process` | stale `skill.html` 감지 시 reminder만 주던 hook을 별도 `codex exec`로 `skill-to-html`을 실행하는 자동 갱신 방식으로 바꿨다. | hook dry-run, node syntax check, repo validator | 새 hook command는 Codex `/hooks`에서 다시 승인해야 적용될 수 있다. |
| 2026-05-11 | repo | Hook validation added | `active process -> active process` | 스킬 생성/수정 후 hook이 repo 기본 validator, 변경된 스킬의 custom validator, repo validator를 실행하고 실패 결과를 additional context로 알려주도록 했다. | hook dry-run, node syntax check, repo validator | validator 실패는 작업 중간 상태를 막지 않도록 advisory context로만 전달한다. |
| 2026-05-11 | repo | Documentation consistency validation added | `active process -> active process` | repo validator가 현재 스킬 목록과 README, AGENTS, project snippets, history registry, validator 명령의 이름과 경로 일치 여부를 검사하도록 보강했다. | repo validator | 새 스킬이나 rename 시 문서 누락을 validator에서 잡는다. |
| 2026-05-12 | repo | Common skill HTML validation added | `active process -> active process` | 모든 `skills/*/skill.html`이 portable static HTML, desktop-centered layout, diagram-rich sections, file-pair map, validation, misuse guardrails를 갖췄는지 공통으로 검사하는 TypeScript validator를 추가했다. | common HTML validator, repo validator, Playwright render check | hook이나 sync-docs 검증 흐름에 포함할 필요가 있으면 추가한다. |
| 2026-05-11 | repo | Docs review findings resolved | `active process -> active process` | inspector 기록에 남은 project setup, base snippet, README 검증 흐름, skill-to-html 경로 예시를 현재 repo 구조에 맞게 정리했다. | repo validator, skill-to-html validators, Chrome headless render | none |
